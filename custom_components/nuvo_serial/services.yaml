configure_time:
  fields:
    device_id:
      required: true
      selector:
        device:
          integration: "nuvo_serial"
    datetime:
      required: true
      selector:
        datetime:

snapshot:
  fields:
    entity_id:
      required: true
      selector:
        entity:
          integration: "nuvo_serial"
          domain: "media_player"
          multiple: true

restore:
  fields:
    entity_id:
      required: true
      selector:
        entity:
          integration: "nuvo_serial"
          domain: "media_player"
          multiple: true

party_on:
  fields:
    entity_id:
      required: true
      selector:
        entity:
          integration: "nuvo_serial"
          domain: "media_player"

party_off:
  fields:
    entity_id:
      required: true
      selector:
        entity:
          integration: "nuvo_serial"
          domain: "media_player"

simulate_play_pause_button:
  fields:
    entity_id:
      required: true
      selector:
        entity:
          integration: "nuvo_serial"
          domain: "media_player"

simulate_prev_button:
  fields:
    entity_id:
      required: true
      selector:
        entity:
          integration: "nuvo_serial"
          domain: "media_player"

simulate_next_button:
  fields:
    entity_id:
      required: true
      selector:
        entity:
          integration: "nuvo_serial"
          domain: "media_player"
